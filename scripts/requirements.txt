# update_meteo_monde_cep.py (source CEP/ECMWF réelle, grille mondiale)
ecmwf-opendata>=0.3.8,<1
numpy>=2.2,<3
eccodes>=2.38,<3
eccodeslib>=2.39,<3

# update_meteo_monde.py (ancienne source Open-Meteo, gardée en secours) :
# n'utilise que la bibliothèque standard, aucune dépendance.
